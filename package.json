{
  "name": "sum-mjschrader307",
  "version": "1.0.0",
  "description": "The program to compute the sum of a list of numbers in `code.js` is incorrect\r (and the tests in `code.test.js` are failing). Correct the code such that it\r passes the tests.",
  "main": "code.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "jsverify": "^0.8.4"
  }
}
